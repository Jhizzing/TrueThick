{
  "name": "truethick",
  "version": "3.2.0",
  "private": true,
  "description": "Structural orientation and true thickness calculator. Static files, no runtime dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
